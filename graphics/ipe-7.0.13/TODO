MAIN GOAL:
    edit eps files, formulas, russian language

is libs really shared?
    libipelua YES

print/freetype to depends?

pdflatex to run-depends:
    print/texlive/texmf,-full

fix pdflatex fonts problem

    add following to document preamble:
	\usepackage[utf8]{inputenc}
	\usepackage[russian]{babel}
	\usepackage{pscyr}
    but math symbols problem :(

low priority:
    hide menu -> help -> keyboard: not so easy
    lua ipe module (symlink?): don't works :(
    fix ipe-update-master:
	fix lua ipe module
