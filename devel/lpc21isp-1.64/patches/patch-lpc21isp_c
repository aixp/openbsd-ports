$OpenBSD$
--- lpc21isp.c.orig	Mon Jan 19 22:34:59 2009
+++ lpc21isp.c	Tue Oct  4 16:45:44 2011
@@ -38,6 +38,7 @@ Portions Copyright (c) by Aeolus Development 2004 http
 #include "adprog.h"
 #include "lpcprog.h"
 #include "lpcterm.h"
+#include <errno.h>
 
 /*
 Change-History:
@@ -367,7 +368,7 @@ static void OpenSerialPort(ISP_ENVIRONMENT *IspEnviron
 #ifdef __APPLE__
 #define NEWTERMIOS_SETBAUDARTE(bps) IspEnvironment->newtio.c_ispeed = IspEnvironment->newtio.c_ospeed = bps;
 
-#elif __FreeBSD__
+#elif __FreeBSD__ || __OpenBSD__
 
 	if(cfsetspeed(&IspEnvironment->newtio,(speed_t) strtol(IspEnvironment->baud_rate,NULL,10))) {
                   DebugPrintf(1, "baudrate %s not supported\n", IspEnvironment->baud_rate);
